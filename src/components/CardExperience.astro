---
const { ocupation, company, startDate, endDate, description } = Astro.props;
---

<!-- <div class="flex flex-col gap-6">
  <div class="flex flex-col md:flex-row gap-4">
    <div class="flex flex-col gap-2">
      <h3 class="text-xl font-semibold text-slate-900">{ocupation}</h3>
      <p class="text-sm text-slate-600">
        <b>{company}</b> - {startDate} - {endDate}
      </p>
    </div>
    <p class="text-slate-700">{description}</p>
  </div>
</div> -->
<li class="relative -ms-1.5 flex items-start gap-4">
  <span class="size-3 shrink-0 rounded-full bg-blue-600"></span>

  <div class="-mt-2">
    <time class="text-xs/none font-medium text-gray-700">{startDate} - {endDate}</time>

    <h3 class="text-lg font-bold text-gray-900">{ocupation} - {company}</h3>

    <p class="mt-0.5 text-sm text-gray-700">{description}</p>
  </div>
</li>
