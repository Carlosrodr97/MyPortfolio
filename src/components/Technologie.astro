---
const { logo, name } = Astro.props;
---

<div
  class="flex gap-2 items-center w-fit px-3 py-2 rounded-lg bg-slate-800 text-xs lg:text-sm"
>
  <img class="w-5 max-sm:w-4" src={logo.src} alt={name} />
  <p>{name}</p>
</div>